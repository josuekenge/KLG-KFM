# NPM Configuration for Netlify Deployment

# Allow legacy peer dependencies
legacy-peer-deps=true

# Skip optional dependencies that might fail
optional=false

# Don't fail on engine mismatch (some packages require Node 20+)
engine-strict=false

# Production mode settings
production=false

