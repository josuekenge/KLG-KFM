# NPM Configuration for Netlify Deployment

# Allow legacy peer dependencies
legacy-peer-deps=true

# Don't fail on engine mismatch (some packages require Node 20+)
engine-strict=false

# Install all dependencies including optional ones (needed for lightningcss)
# Don't skip optional dependencies
include=optional

